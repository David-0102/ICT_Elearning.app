import React from 'react';

const StudentDashboard = () => {
    return (
        <div>
            <h1>Student Dashboard</h1>
            {/* Add functionality for students to view and enroll in courses */}
        </div>
    );
};

export default StudentDashboard;
